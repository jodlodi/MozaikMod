# The fabric version will be implemented at some point, gave up getting that part done on time when I hit several neoForge only features.

`main` branch is always whatever the latest vanilla version is, assuming modloaders are available.
